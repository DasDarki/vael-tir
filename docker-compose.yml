services:
  frontend:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: vael-tir-frontend
    ports:
      - "2280:80"
    networks:
      - shared-network
    restart: unless-stopped

networks:
  shared-network:
    external: true